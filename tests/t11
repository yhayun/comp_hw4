//should fail (in line 12 NOT 10)
void printOk(bool isOk) {
    if (isOk) print("ok");
}

void main()
{
	if(true)
		int c =5;
	if ( (5*2 + 1 == 11) )
		int d = 6;
	if ( (5*2 + 1 != 11) )
		int v = 6;
    int x = 4;
}