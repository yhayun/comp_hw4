void main() {

	int a;

	switch(2) {
		case 2: return; break;
	};

	switch(a) {
		case 2: return; break;
	};

	byte c;

	switch(2 b) {
		case 2: return; break;
	};

	switch(a) {
		case 2: return; break;
	};
}